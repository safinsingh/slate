# an example slate
# build configuration

# !dep([xlib])

$runner = gcc
!env({
    CFLAGS = -std=c99 -Wall -Werror
})

!run($runner)
!src([
    src/main.c,
    src/util.c,
    src/util.h,
    lang/lex.c,
    lang/lex.h
])

!exec([
    !cmd("echo test"),
    !build(),
])

# !include(cli)
# !include(lang)